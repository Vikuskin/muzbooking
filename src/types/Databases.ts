export interface ServicesDB {
    img: string;
    title: string;
    icon: string;
    description: string;
    id: string;
}

export interface CheckedPlaceDB {
    value: string;
    id: number;
    checked: boolean;
}

export interface CardBeneftsDB {
    img: string;
    description: string;
    id: number;
}